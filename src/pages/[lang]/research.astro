---
import ProjectCard from '../../components/ProjectCard.astro';
import { projects, research, type Lang } from '../../data/site';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getLangPaths, isLang } from '../../lib/i18n';

export function getStaticPaths() {
	return getLangPaths();
}

const { lang: langParam } = Astro.params;
if (!isLang(langParam)) throw new Error('Unsupported language');
const lang: Lang = langParam;
const t = research[lang];
const thesisProjects = projects.filter((project) => project.featured);
const description =
	lang === 'en'
		? 'Research interests of Shiuan-Ting Lin in structured representations, explainable AI, and self-supervised predictive learning.'
		: '林宣廷在結構化表徵、可解釋 AI、自監督與預測式學習的研究方向。';
---

<BaseLayout lang={lang} title={t.eyebrow} description={description}>
	<header class="page-hero shell">
		<h1>{t.eyebrow}</h1>
	</header>

	<section class="section section--tint">
		<div class="shell">
			<header class="section-heading section-heading--compact">
				<h2>{t.thesisTitle}</h2>
			</header>
			<div class="project-grid project-grid--thesis">
				{thesisProjects.map((project) => <ProjectCard project={project} lang={lang} compact />)}
			</div>
		</div>
	</section>

	<section class="section shell">
		<header class="section-heading section-heading--compact">
			<h2>{t.threadsTitle}</h2>
		</header>
		<div class="research-thread-grid">
				{
					t.threads.map((thread) => (
						<article class="research-thread">
							<p class="research-thread__number">{thread.number}</p>
							<h3>{thread.title}</h3>
							<div class="research-thread__body">
								<p>{thread.body}</p>
								<ul class="tag-list">
									{thread.tags.map((tag) => <li class="tag">{tag}</li>)}
								</ul>
							</div>
						</article>
					))
				}
		</div>
	</section>

</BaseLayout>
